# v1.14 set_frame_scroll (overflowDirection setter) — Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development.

**Goal:** 새 MCP tool `set_frame_scroll` 노출 — `FrameNode.overflowDirection` (NONE/HORIZONTAL/VERTICAL/BOTH) batch setter. 시나리오 6의 수동 Figma 토글 prerequisite 제거.

**Architecture:** Reaction이 아니라 frame property setter라 builder 변경 없음. 새 Zod schema + 새 CommandName + plugin handler 1개만 추가. partial-success 패턴은 `create_reactions`와 동일.

**Source spec:** `docs/superpowers/specs/2026-05-20-v1.14-set-frame-scroll-design.md`

---

## File Map

| File | Change |
|---|---|
| `src/mcp-server/tools.ts` | `OverflowDirectionEnum` + `SetFrameScrollInput` Zod 스키마 + export |
| `src/mcp-server/types.ts` | `CommandName` union에 `"SET_FRAME_SCROLL"` 추가 |
| `src/mcp-server/index.ts` | TOOLS 배열에 `set_frame_scroll` 항목 추가 + `SetFrameScrollInput` import |
| `src/figma-plugin/code.ts` | `Command` union에 SET_FRAME_SCROLL variant + dispatch 분기 + `handleSetFrameScroll` 함수 |
| `tests/tools.test.ts` | Zod 검증 케이스: valid batch / invalid direction / empty frames / empty frameId |
| `README.md` | tool 항목 1행 + 시나리오 19 (3 sub-cases + 1 error case) + 시나리오 6 setup 안내 갱신 |

---

## Task 1: Zod 스키마 + 단위 테스트

**Files:**
- Modify: `src/mcp-server/tools.ts`
- Modify: `tests/tools.test.ts`

- [ ] **Step 1: Write failing tests**

`tests/tools.test.ts` 끝에 추가:

```typescript
describe("SetFrameScrollInput", () => {
  it("accepts single frame with VERTICAL", () => {
    const r = SetFrameScrollInput.safeParse({
      frames: [{ frameId: "1:1", direction: "VERTICAL" }],
    });
    expect(r.success).toBe(true);
  });
  it("accepts batch with all 4 directions", () => {
    const r = SetFrameScrollInput.safeParse({
      frames: [
        { frameId: "1:1", direction: "NONE" },
        { frameId: "1:2", direction: "HORIZONTAL" },
        { frameId: "1:3", direction: "VERTICAL" },
        { frameId: "1:4", direction: "BOTH" },
      ],
    });
    expect(r.success).toBe(true);
  });
  it("rejects empty frames array", () => {
    const r = SetFrameScrollInput.safeParse({ frames: [] });
    expect(r.success).toBe(false);
  });
  it("rejects invalid direction value", () => {
    const r = SetFrameScrollInput.safeParse({
      frames: [{ frameId: "1:1", direction: "DIAGONAL" }],
    });
    expect(r.success).toBe(false);
  });
  it("rejects empty frameId", () => {
    const r = SetFrameScrollInput.safeParse({
      frames: [{ frameId: "", direction: "VERTICAL" }],
    });
    expect(r.success).toBe(false);
  });
  it("rejects missing direction", () => {
    const r = SetFrameScrollInput.safeParse({
      frames: [{ frameId: "1:1" }],
    });
    expect(r.success).toBe(false);
  });
});
```

`SetFrameScrollInput`을 기존 import 라인에 추가.

- [ ] **Step 2: Run, verify FAIL**

```bash
npm test -- tests/tools.test.ts
```

- [ ] **Step 3: Implement schema**

`src/mcp-server/tools.ts` 끝부분 (ClearReactionsInput 정의 다음, type 재export 직전)에 추가:

```ts
const OverflowDirectionEnum = z.enum(["NONE", "HORIZONTAL", "VERTICAL", "BOTH"]);

const SetFrameScrollEntry = z.object({
  frameId: z.string().min(1),
  direction: OverflowDirectionEnum,
});

export const SetFrameScrollInput = z.object({
  frames: z.array(SetFrameScrollEntry).min(1),
});
```

파일 끝의 type alias export 블록에도 추가:
```ts
export type SetFrameScrollInput = z.infer<typeof SetFrameScrollInput>;
```

- [ ] **Step 4: Run, verify PASS**

```bash
npm test
```

전체 통과 확인.

- [ ] **Step 5: Commit**

```bash
git add src/mcp-server/tools.ts tests/tools.test.ts
git commit -m "feat(tools): SetFrameScrollInput Zod schema (NONE/H/V/BOTH)"
```

---

## Task 2: CommandName + tool 등록

**Files:**
- Modify: `src/mcp-server/types.ts`
- Modify: `src/mcp-server/index.ts`

- [ ] **Step 1: Extend CommandName**

`src/mcp-server/types.ts`:
```ts
export type CommandName =
  | "GET_CANVAS_OVERVIEW"
  | "FIND_NODES"
  | "CREATE_REACTIONS"
  | "LIST_REACTIONS"
  | "CLEAR_REACTIONS"
  | "SET_FRAME_SCROLL";
```

- [ ] **Step 2: Register tool in index.ts**

`src/mcp-server/index.ts` import 블록에 `SetFrameScrollInput` 추가:
```ts
import {
  GetCanvasOverviewInput,
  FindNodesInput,
  CreateReactionsInput,
  ListReactionsInput,
  ClearReactionsInput,
  SetFrameScrollInput,
} from "./tools.js";
```

TOOLS 배열 끝에 추가:
```ts
{
  name: "set_frame_scroll",
  description:
    "Configure a frame's scroll behavior (overflowDirection) for prototype mode. " +
    "Accepts a batch of { frameId, direction }; each succeeds or fails independently. " +
    "Direction values: NONE (no scrolling), HORIZONTAL, VERTICAL, BOTH.",
  schema: SetFrameScrollInput,
  command: "SET_FRAME_SCROLL" as CommandName,
},
```

- [ ] **Step 3: Run typecheck**

```bash
npm run typecheck
```

Expected: 통과. (handler가 아직 없어도 dispatch는 plugin 쪽이므로 타입 에러 없음.)

- [ ] **Step 4: Commit**

```bash
git add src/mcp-server/types.ts src/mcp-server/index.ts
git commit -m "feat(mcp): register set_frame_scroll tool + SET_FRAME_SCROLL command"
```

---

## Task 3: Plugin handler

**Files:**
- Modify: `src/figma-plugin/code.ts`

- [ ] **Step 1: Extend Command union**

`Command` union 끝에 새 variant 추가:
```ts
| {
    type: "SET_FRAME_SCROLL";
    params: {
      frames: Array<{
        frameId: string;
        direction: "NONE" | "HORIZONTAL" | "VERTICAL" | "BOTH";
      }>;
    };
  }
```

- [ ] **Step 2: Add dispatch case**

`dispatch` 함수의 switch에 추가 (CLEAR_REACTIONS 다음):
```ts
case "SET_FRAME_SCROLL": return { status: "ok", result: await handleSetFrameScroll(params) };
```

- [ ] **Step 3: Implement handleSetFrameScroll**

파일 끝 (handleClearReactions 다음)에 추가:

```ts
async function handleSetFrameScroll(params: {
  frames: Array<{ frameId: string; direction: "NONE" | "HORIZONTAL" | "VERTICAL" | "BOTH" }>;
}) {
  await figma.loadAllPagesAsync();
  const results: Array<{ frameId: string; status: "success" | "error"; error?: string }> = [];
  let successCount = 0;
  let errorCount = 0;
  for (const { frameId, direction } of params.frames) {
    try {
      const node = figma.getNodeById(frameId);
      if (!node) throw new Error(`Frame not found: ${frameId}`);
      if (node.type !== "FRAME") {
        throw new Error(`Node is not a FRAME: ${node.name} (type: ${node.type})`);
      }
      (node as FrameNode).overflowDirection = direction;
      results.push({ frameId, status: "success" });
      successCount++;
    } catch (e: any) {
      results.push({ frameId, status: "error", error: e?.message ?? String(e) });
      errorCount++;
    }
  }
  return { results, successCount, errorCount };
}
```

- [ ] **Step 4: Build plugin**

```bash
npm run build:plugin
```

Expected: `dist/figma-plugin/code.js` 생성. 에러 없음.

- [ ] **Step 5: Run typecheck + tests**

```bash
npm run typecheck
npm test
```

전체 통과 확인.

- [ ] **Step 6: Commit**

```bash
git add src/figma-plugin/code.ts
git commit -m "feat(plugin): handleSetFrameScroll mutates FrameNode.overflowDirection"
```

---

## Task 4: README + 라이브 검증 + release

**Files:**
- Modify: `README.md`
- 사용자가 새 fixture (MCP_Test_09 또는 비슷한 이름) 생성 후 frame ID 제공
- Create (temp): `scenario19-e2e.mjs` — 검증 후 삭제

- [ ] **Step 1: README tool 항목 추가**

기존 `| clear_reactions ...` 행 뒤에 추가:
```markdown
| `set_frame_scroll` | **Write**: configure `overflowDirection` on one or more FRAME nodes (`NONE` / `HORIZONTAL` / `VERTICAL` / `BOTH`). Each frame succeeds or fails independently. Use before `create_reactions` for Scroll To to ensure the source frame is scrollable. |
```

- [ ] **Step 2: README 시나리오 19 추가**

마지막 시나리오 뒤에:

```markdown
- [ ] **19. Configure frame scroll behavior (`set_frame_scroll`)**:
  Setup: a new test section with three frames: `vTall` (tall — child content exceeds frame height), `hWide` (wide — child content exceeds frame width), `noScroll` (small).
  (a) Ask: "vTall frame 세로로 스크롤되게 해줘". Expected: successCount=1. Figma → Prototype panel shows "Vertical scrolling". Play mode: vertical drag scrolls content.
  (b) Ask: "vTall 스크롤 꺼줘". Expected: Overflow Behavior = "No scrolling". Play: overflowing content is clipped, no scroll.
  (c) Ask: "hWide 가로로 스크롤". Expected: "Horizontal scrolling". Play: horizontal scroll works.
  (d) Error case: pass a non-existent frameId. Expected: status=error, errorCount=1, error message contains "Frame not found".
```

- [ ] **Step 3: README 시나리오 6 setup 안내 갱신**

기존 시나리오 6 Setup 라인:
> Setup: Create a tall frame with Scroll behavior set to "Vertical scrolling" (Figma Inspector → Frame → Overflow: Vertical). Inside it, place a section node named "Pricing". Outside any scrollable frame, place another node named "Footer".

다음으로 교체:
> Setup: Create a tall frame containing a section node named "Pricing", and another node "Footer" outside any scrollable frame. Set the tall frame's Overflow Behavior to "Vertical scrolling" — either via Figma Inspector → Frame → Overflow: Vertical, **or via the `set_frame_scroll` tool** (`{ frames: [{ frameId, direction: "VERTICAL" }] }`).

- [ ] **Step 4: 사용자에게 fixture 생성 요청**

사용자가 새 section을 만들고 frame ID를 제공할 때까지 대기. 사용자 응답 후 받은 ID로 진행.

- [ ] **Step 5: Live verify scenario 19 — relay-bypass script**

`scenario19-e2e.mjs`:

```javascript
import WebSocket from "ws";
const ws = new WebSocket("ws://localhost:3055");
const channel = "test1";
const correlations = new Map();

const VTALL = "<provided by user>";
const HWIDE = "<provided by user>";
const NOSCROLL = "<provided by user>";  // 또는 임의의 RECTANGLE id (error case)
const BAD_ID = "999:99999";

function send(command, params) { /* ... 기존 패턴 ... */ }

ws.on("open", async () => {
  ws.send(JSON.stringify({ type: "join", channel }));
  await new Promise(r => setTimeout(r, 200));
  const cases = [
    { name: "19a vTall=VERTICAL", frames: [{ frameId: VTALL, direction: "VERTICAL" }] },
    { name: "19b vTall=NONE", frames: [{ frameId: VTALL, direction: "NONE" }] },
    { name: "19c hWide=HORIZONTAL", frames: [{ frameId: HWIDE, direction: "HORIZONTAL" }] },
    { name: "19d bad id error", frames: [{ frameId: BAD_ID, direction: "VERTICAL" }] },
  ];
  for (const c of cases) {
    const r = await send("SET_FRAME_SCROLL", { frames: c.frames });
    console.log(`--- ${c.name} ---`, JSON.stringify(r));
  }
  ws.close();
});

ws.on("message", /* ... */);
```

각 결과에서:
- (a)/(b)/(c): successCount=1, errorCount=0
- (d): successCount=0, errorCount=1, error message에 "Frame not found"

그리고 Figma에서 직접 Prototype 탭 확인 → Overflow Behavior 값이 시나리오별로 정확히 바뀌었는지. Play 모드 진입해서 실제 스크롤 동작 확인.

- [ ] **Step 6: Live 통과하면 README `[x]` 마킹 + temp script 삭제**

```bash
rm scenario19-e2e.mjs
git add README.md
git commit -m "docs: README v1.14 — set_frame_scroll tool + scenario 19 [x]"
```

- [ ] **Step 7: Version bump + tag + release**

`package.json` 0.12.0 → 0.13.0. `src/mcp-server/index.ts`의 Server 버전도 0.12.0 → 0.13.0.

```bash
git add package.json src/mcp-server/index.ts
git commit -m "chore: v0.13.0 — set_frame_scroll (overflowDirection setter) live-verified, bump"

git tag -a v0.13.0 HEAD -m "v0.13.0 — set_frame_scroll: configure FrameNode.overflowDirection (NONE/H/V/BOTH) batch setter"
git push origin main
git push origin v0.13.0

gh release create v0.13.0 \
  --title "v0.13.0 — set_frame_scroll (overflowDirection setter)" \
  --notes "<release notes>"
```

- [ ] **Step 8: Memory update**

`memory/repo-and-releases.md`에 v0.13.0 entry 추가.

---

## Risk + Fallback

- overflowDirection 설정해도 child가 frame bounds 안 넘으면 prototype play에서 스크롤바 안 보임 — 사용자 fixture 책임으로 README에 명시.
- Runtime mismatch 위험 낮음 (`readonly` 키워드 없는 단순 mutable property). 그래도 시나리오 19(a) 첫 호출에서 1차 확인.
- Plugin reload 필요 — Task 3 build:plugin 후 사용자에게 reload 요청 전 README 작업 먼저 진행.
- 회귀 위험 낮음 — 다른 tool / builder / handler에 영향 없음. 기존 123 tests 그대로 통과 예상.
